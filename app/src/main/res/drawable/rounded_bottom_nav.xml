<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#FFFFFF" /> <!-- đổi màu theo ý bạn -->

    <!-- Bo góc -->
    <corners android:radius="30dp" />

    <!-- Đổ bóng hoặc viền (tùy chọn) -->
    <stroke
        android:width="1dp"
        android:color="#CDC8C8" />

    <!-- Khoảng cách bên trong -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
