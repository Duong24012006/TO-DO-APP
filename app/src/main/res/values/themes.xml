<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TODOAPP" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TODOAPP" parent="Base.Theme.TODOAPP" />

    <style name="Widget.App.Button.Filter" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/filter_button_background</item>
        <item name="android:textColor">@color/filter_button_text_color</item>
        <item name="cornerRadius">14dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MyShapeAppearance" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">40dp</item>
    </style>

    <style name="MyChipStyle" parent="Widget.Material3.Chip.Suggestion">
        <!-- Bạn có thể tùy chỉnh thêm các thuộc tính khác ở đây nếu muốn -->
        <!-- Ví dụ: <item name="android:textColor">@color/your_color</item> -->
    </style>

</resources>